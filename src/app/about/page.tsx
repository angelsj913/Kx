import CompanyPageContent from "@/components/landing/CompanyPageContent";

export const metadata = { title: "회사소개 · ZEFF AI" };

export default function AboutPage() {
  return <CompanyPageContent tab="about" />;
}
