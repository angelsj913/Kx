import CompanyPageContent from "@/components/landing/CompanyPageContent";

export const metadata = { title: "발전 가능성 · ZEFF AI" };

export default function VisionPage() {
  return <CompanyPageContent tab="vision" />;
}
