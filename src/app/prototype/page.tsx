import CompanyPageContent from "@/components/landing/CompanyPageContent";

export const metadata = { title: "개발중인 프로토타입 · ZEFF AI" };

export default function PrototypePage() {
  return <CompanyPageContent tab="prototype" />;
}
